
export default class User {
    constructor(id, firstName, lastName, email, dateOfBirth, createdAt) {
      this.id = id;
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
      this.dateOfBirth = dateOfBirth;
      this.createdAt = createdAt;
    }
  }
  